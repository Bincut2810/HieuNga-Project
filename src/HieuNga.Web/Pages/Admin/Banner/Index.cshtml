@page "/admin/banner"
@model HieuNga.Web.Pages.Admin.Banner.IndexModel

<div class="flex flex-wrap items-center justify-between gap-4">
    <h1 class="text-2xl font-bold">Banner</h1>
    <a href="/admin/banner/them" class="bg-honda-red text-white px-4 py-2 rounded-lg font-semibold text-sm">+ Thêm banner</a>
</div>
<div class="mt-8 grid md:grid-cols-2 gap-6">
    @foreach (var b in Model.Items)
    {
        <div class="bg-white border rounded-2xl overflow-hidden">
            <div class="aspect-video bg-gray-100">
                <img src="@b.ImageUrl" alt="@b.Title" class="w-full h-full object-cover" />
            </div>
            <div class="p-5 flex justify-between items-start gap-4">
                <div>
                    <h3 class="font-bold">@b.Title</h3>
                    <p class="text-sm text-gray-500 mt-1">@b.Subtitle</p>
                </div>
                <a href="/admin/banner/sua/@b.Id" class="text-honda-red font-semibold text-sm shrink-0">Sửa</a>
            </div>
        </div>
    }
</div>
